name: dl_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.0.0
      - torchvision
      - ultralytics>=8.1.0
      - opencv-python
      - pillow
      - imageio
      - numpy
      - matplotlib
      - seaborn
      - scikit-learn
      - pandas
      - tqdm
      - pyyaml
      - gradio
      - easyocr>=1.6.2
      - paddleocr
      - paddlepaddle==2.5.2
      - pytesseract
      - json5
